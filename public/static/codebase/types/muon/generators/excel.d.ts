import { IFormula, parseContext } from "../types";
export declare function print(f: IFormula, ctx: parseContext): string;
export declare function position(row: number, col: number, flags: number): string;
